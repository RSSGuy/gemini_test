specVersion: "v1"
serviceId: "geminitest"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "geminitest-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
